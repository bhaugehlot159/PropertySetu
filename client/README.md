# Client Structure

- `pages/` : Static portal pages (customer/admin) for current HTML demo.
- `components/` : Reusable UI blocks (reserved for future split).
- `services/` : API service wrappers.
- `utils/` : Utility helpers.
- `js/` : Client-side scripts.
