# PropertySetu Backend Root

Professional backend entrypoint layer.

## Start

```bash
cd backend
npm start
```

This starts the existing live API server from:

`server/server.js`

No legacy code is removed. This folder is an upgrade layer for cleaner backend root usage.

